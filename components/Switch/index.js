import styled from "styled-components"


const BTN = styled.button`


`


const Switch = (props) => {
    
    let new_state = state

    const MakeState = () => {

    }
    
    
    return(
        <BNT onClick={() => MakeState()}>
            {button_state}
        </BNT>
    )
}

export default Switch